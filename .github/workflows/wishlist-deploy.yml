name: Wishlist CI / Deploy

on:
  pull_request:
    branches:
      - develop
      - main
    paths:
      - wishlist/**
      - .github/workflows/wishlist-deploy.yml
      - tests/test_wishlist_deployment_workflow.py
  push:
    branches:
      - develop
      - main
    tags:
      - wishlist-v*
    paths:
      - wishlist/**
      - .github/workflows/wishlist-deploy.yml
      - tests/test_wishlist_deployment_workflow.py
  workflow_dispatch:

concurrency:
  group: wishlist-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref_type == 'branch' }}

permissions:
  contents: read

env:
  UV_VERSION: "0.11.32"
  IMAGE_NAME: ghcr.io/mickeyshide/flashmarket-wishlist

jobs:
  ruff:
    name: Ruff warnings
    runs-on: ubuntu-latest
    continue-on-error: true
    defaults:
      run:
        working-directory: wishlist
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          version: ${{ env.UV_VERSION }}
          enable-cache: true
          cache-dependency-glob: wishlist/uv.lock

      - name: Install Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --locked --all-groups

      - name: Run Ruff as warnings
        run: uv run ruff check .

  pytest:
    name: Pytest suite
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: wishlist
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          version: ${{ env.UV_VERSION }}
          enable-cache: true
          cache-dependency-glob: wishlist/uv.lock

      - name: Install Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --locked --all-groups

      - name: Run unit & integration tests
        run: uv run pytest -q -p no:cacheprovider

  image:
    name: Build Wishlist image
    needs:
      - pytest
    runs-on: ubuntu-latest
    outputs:
      digest: ${{ steps.build.outputs.digest }}
    permissions:
      contents: read
      packages: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=develop,enable=${{ github.ref == 'refs/heads/develop' }}
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=ref,event=tag
            type=ref,event=pr
            type=sha,prefix=sha-

      - name: Build and publish image
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./wishlist/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=flashmarket-wishlist
          cache-to: type=gha,mode=max,scope=flashmarket-wishlist

  deploy:
    name: Deploy production
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.ref == 'refs/heads/main' ||
      startsWith(github.ref, 'refs/tags/wishlist-v')
    needs:
      - image
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: read
      packages: read
    env:
      DEPLOY_HOST: ${{ vars.DEPLOY_HOST || '176.108.242.100' }}
      DEPLOY_PORT: ${{ vars.DEPLOY_PORT || '22' }}
      DEPLOY_USER: ${{ vars.DEPLOY_USER || 'user1' }}
      DEPLOY_PATH: ${{ vars.WISHLIST_DEPLOY_PATH }}
      SSH_PRIVATE_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
      SSH_KNOWN_HOSTS: ${{ secrets.DEPLOY_KNOWN_HOSTS }}
      GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GHCR_USERNAME: ${{ github.actor }}
      INFRA_USER: ${{ vars.INFRA_USER || 'shide' }}
      WISHLIST_IMAGE: ghcr.io/mickeyshide/flashmarket-wishlist@${{ needs.image.outputs.digest }}
      WISHLIST_CORS_ORIGINS: ${{ vars.WISHLIST_CORS_ORIGINS }}
      GATEWAY_DOMAIN: ${{ vars.GATEWAY_DOMAIN || 'flashmarket.shide.world' }}
      DEPLOY_POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
      DEPLOY_RABBITMQ_PASSWORD: ${{ secrets.RABBITMQ_PASSWORD }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Validate deployment configuration
        shell: bash
        run: |
          DEPLOY_HOST="${DEPLOY_HOST:-176.108.242.100}"
          DEPLOY_PORT="${DEPLOY_PORT:-22}"
          DEPLOY_USER="${DEPLOY_USER:-user1}"
          INFRA_USER="${INFRA_USER:-shide}"

          if [[ ! "$DEPLOY_HOST" =~ ^[A-Za-z0-9.-]+$ ]]; then
            echo "::error::DEPLOY_HOST must be a hostname or IPv4 address"
            exit 1
          fi
          if [[ ! "$DEPLOY_USER" =~ ^[A-Za-z_][A-Za-z0-9_-]*$ ]]; then
            echo "::error::DEPLOY_USER is invalid"
            exit 1
          fi
          if [[ -z "$DEPLOY_PATH" ]]; then
            DEPLOY_PATH="/home/${DEPLOY_USER}/flashmarket-wishlist"
            echo "DEPLOY_PATH=$DEPLOY_PATH" >> "$GITHUB_ENV"
          fi
          if [[ ! "$DEPLOY_PORT" =~ ^[0-9]+$ ]] ||
            (( DEPLOY_PORT < 1 || DEPLOY_PORT > 65535 )); then
            echo "::error::DEPLOY_PORT must be between 1 and 65535"
            exit 1
          fi
          if [[ ! "$DEPLOY_PATH" =~ ^/[A-Za-z0-9._/-]+$ ]]; then
            echo "::error::DEPLOY_PATH must be an absolute Unix path"
            exit 1
          fi
          if [[ ! "$GATEWAY_DOMAIN" =~ ^[A-Za-z0-9.-]+$ ]]; then
            echo "::error::GATEWAY_DOMAIN must contain only a DNS hostname"
            exit 1
          fi
          if [[ ! "$INFRA_USER" =~ ^[A-Za-z_][A-Za-z0-9_-]*$ ]]; then
            echo "::error::INFRA_USER is invalid"
            exit 1
          fi
          if [[ -z "$SSH_PRIVATE_KEY" ]]; then
            echo "::error::DEPLOY_SSH_KEY is required"
            exit 1
          fi
          if [[ -z "$DEPLOY_POSTGRES_PASSWORD" ]]; then
            echo "::error::POSTGRES_PASSWORD is required"
            exit 1
          fi
          if [[ -z "$DEPLOY_RABBITMQ_PASSWORD" ]]; then
            echo "::error::RABBITMQ_PASSWORD is required"
            exit 1
          fi
          if [[ -z "${{ needs.image.outputs.digest }}" ]]; then
            echo "::error::Wishlist image digest is missing"
            exit 1
          fi

      - name: Configure SSH
        shell: bash
        run: bash scripts/configure-deploy-ssh.sh

      - name: Render production environment
        shell: bash
        run: |
          environment_file="$RUNNER_TEMP/flashmarket-wishlist.env"
          cors_origins="${WISHLIST_CORS_ORIGINS:-[\"https://${GATEWAY_DOMAIN}\"]}"
          umask 077
          {
            printf 'WISHLIST_IMAGE=%s\n' "$WISHLIST_IMAGE"
            printf 'WISHLIST_ENVIRONMENT=production\n'
            printf 'WISHLIST_DATABASE_URL=postgresql+asyncpg://%s:%s@shide-postgres:5432/wishlist\n' "$INFRA_USER" "$DEPLOY_POSTGRES_PASSWORD"
            printf 'WISHLIST_RABBITMQ_URL=amqp://%s:%s@shide-rabbitmq:5672/flashmarket\n' "$INFRA_USER" "$DEPLOY_RABBITMQ_PASSWORD"
            printf 'WISHLIST_CORS_ORIGINS=%s\n' "$cors_origins"
            printf 'WISHLIST_TRUSTED_HOSTS=["%s","wishlist.%s","localhost","127.0.0.1","wishlist","gateway"]\n' "$GATEWAY_DOMAIN" "$GATEWAY_DOMAIN"
            printf 'WISHLIST_ALLOW_INSECURE_INTERNAL_SERVICES=true\n'
            printf 'WISHLIST_JWT_PUBLIC_KEY_DIR=/var/lib/flashmarket/keys/public\n'
            printf 'WISHLIST_JWT_ALGORITHM=EdDSA\n'
            printf 'WISHLIST_JWT_ISSUER=flashmarket-auth\n'
            printf 'WISHLIST_JWT_AUDIENCE=flashmarket-api\n'
            printf 'WISHLIST_DEBUG=false\n'
            printf 'WISHLIST_DOCS_ENABLED=false\n'
          } > "$environment_file"
          chmod 600 "$environment_file"

      - name: Upload deployment files
        shell: bash
        run: |
          target="${DEPLOY_USER}@${DEPLOY_HOST}"
          ssh -p "$DEPLOY_PORT" "$target" "mkdir -p '$DEPLOY_PATH'"
          scp -P "$DEPLOY_PORT" \
            wishlist/docker-compose.deploy.yml \
            "$target:$DEPLOY_PATH/docker-compose.yml"
          ssh -p "$DEPLOY_PORT" "$target" \
            "umask 077; cat > '$DEPLOY_PATH/.env.next' && mv '$DEPLOY_PATH/.env.next' '$DEPLOY_PATH/.env'" \
            < "$RUNNER_TEMP/flashmarket-wishlist.env"

      - name: Log server in to GHCR
        shell: bash
        run: |
          target="${DEPLOY_USER}@${DEPLOY_HOST}"
          docker_config="${DEPLOY_PATH}/.docker-wishlist"
          ssh -p "$DEPLOY_PORT" "$target" "install -d -m 700 '$docker_config'"
          printf '%s' "$GHCR_TOKEN" |
            ssh -p "$DEPLOY_PORT" "$target" \
              "DOCKER_CONFIG='$docker_config' docker login ghcr.io --username '$GHCR_USERNAME' --password-stdin"

      - name: Deploy exact image digest
        shell: bash
        run: |
          target="${DEPLOY_USER}@${DEPLOY_HOST}"
          timeout --signal=TERM --kill-after=30s 2400 \
            ssh -p "$DEPLOY_PORT" "$target" \
            "DEPLOY_PATH='$DEPLOY_PATH' WISHLIST_IMAGE='$WISHLIST_IMAGE' bash -s" <<'REMOTE'
          set -Eeuo pipefail
          exec 9>/tmp/flashmarket-production-deploy.lock
          flock -w 1800 9
          cd "$DEPLOY_PATH"
          export DOCKER_CONFIG="$DEPLOY_PATH/.docker-wishlist"
          trap 'docker logout ghcr.io >/dev/null 2>&1 || true; rm -f -- "$DOCKER_CONFIG/config.json"; rmdir -- "$DOCKER_CONFIG" 2>/dev/null || true' EXIT

          if [[ ! -f .env ]]; then
            echo "Missing $DEPLOY_PATH/.env"
            exit 1
          fi

          export WISHLIST_IMAGE
          docker compose -f docker-compose.yml --env-file .env config --quiet
          docker compose -f docker-compose.yml --env-file .env pull

          echo 'Applying Wishlist database migrations...'
          timeout 120 docker compose -f docker-compose.yml --env-file .env run --rm --no-deps api migrate </dev/null
          echo 'Database migrations complete.'

          echo 'Starting Wishlist API, consumer, and outbox worker...'
          docker compose -f docker-compose.yml --env-file .env up \
            -d \
            --no-build \
            --force-recreate \
            --remove-orphans \
            api consumer outbox

          api_container="$(docker compose -f docker-compose.yml --env-file .env ps -q api)"
          consumer_container="$(docker compose -f docker-compose.yml --env-file .env ps -q consumer)"
          outbox_container="$(docker compose -f docker-compose.yml --env-file .env ps -q outbox)"
          if [[ -z "$consumer_container" || -z "$outbox_container" ]]; then
            docker compose -f docker-compose.yml --env-file .env logs --tail 100 consumer outbox
            exit 1
          fi
          initial_consumer_restarts="$(docker inspect --format '{{.RestartCount}}' "$consumer_container")"
          initial_outbox_restarts="$(docker inspect --format '{{.RestartCount}}' "$outbox_container")"
          for attempt in $(seq 1 60); do
            health="$(
              docker inspect \
                --format '{{if .State.Health}}{{.State.Health.Status}}{{else}}{{.State.Status}}{{end}}' \
                "$api_container"
            )"
            if [[ "$health" == "healthy" ]]; then
              break
            fi
            sleep 2
          done
          final_health="$(docker inspect --format '{{if .State.Health}}{{.State.Health.Status}}{{else}}{{.State.Status}}{{end}}' "$api_container")"
          if [[ "$final_health" != "healthy" ]]; then
            docker compose -f docker-compose.yml --env-file .env logs --tail 100 api
            exit 1
          fi

          if [[ -z "$consumer_container" ]] || [[ "$(docker inspect --format '{{.State.Running}}' "$consumer_container")" != "true" ]]; then
            docker compose -f docker-compose.yml --env-file .env logs --tail 100 consumer
            exit 1
          fi
          final_consumer_restarts="$(docker inspect --format '{{.RestartCount}}' "$consumer_container")"
          if [[ "$final_consumer_restarts" != "$initial_consumer_restarts" ]]; then
            echo "Wishlist consumer restarted during deployment verification"
            docker compose -f docker-compose.yml --env-file .env logs --tail 100 consumer
            exit 1
          fi
          for attempt in $(seq 1 30); do
            outbox_health="$(docker inspect --format '{{.State.Health.Status}}' "$outbox_container")"
            [[ "$outbox_health" == "healthy" ]] && break
            sleep 2
          done
          if [[ "$(docker inspect --format '{{.State.Health.Status}}' "$outbox_container")" != "healthy" ]]; then
            docker compose -f docker-compose.yml --env-file .env logs --tail 100 outbox
            exit 1
          fi
          final_outbox_restarts="$(docker inspect --format '{{.RestartCount}}' "$outbox_container")"
          if [[ "$final_outbox_restarts" != "$initial_outbox_restarts" ]]; then
            echo "Wishlist outbox restarted during deployment verification"
            docker compose -f docker-compose.yml --env-file .env logs --tail 100 outbox
            exit 1
          fi
          docker compose -f docker-compose.yml --env-file .env ps -a
          REMOTE

      - name: Verify public Wishlist upstream
        shell: bash
        run: |
          last_error=""
          for _ in $(seq 1 30); do
            if response="$(
              curl --fail --silent --show-error \
                "https://${GATEWAY_DOMAIN}/dev/status/wishlist" 2>&1
            )"; then
              printf '%s\n' "$response"
              exit 0
            fi
            last_error="$response"
            sleep 2
          done
          echo "::error::Wishlist is not reachable through the production gateway"
          printf 'Last curl error: %s\n' "$last_error"
          exit 1
